# LangChain: Structured Output & Tracing (LangSmith)

## 📌 Описание проекта
В этом ноутбуке продемонстрирована работа с **LangChain (LCEL)** для подключения языковой модели OpenAI и получения **структурированного ответа** с помощью `PydanticOutputParser`.

Проект показывает, как:
- подключать LLM через LangChain
- формировать промпты с `PromptTemplate`
- получать строго типизированный результат (Pydantic)
- собирать цепочку `prompt → model → parser`
- отслеживать выполнение запросов через **LangSmith (tracing)**

---

## 🧩 Используемые технологии
- **Python**
- **LangChain (LCEL, langchain_core, langchain_openai)**
- **OpenAI API**
- **Pydantic**
- **LangSmith (Tracing / Observability)**
- **Google Colab (Secrets / environment variables)**

---

## 🛠️ Что реализовано

- ✅ Подключение модели `gpt-4o-mini` через `ChatOpenAI`
- ✅ Использование `PromptTemplate` для формирования запроса
- ✅ Структурированный вывод через `PydanticOutputParser`
- ✅ Получение результата в виде Python-объекта
- ✅ Безопасная работа с API-ключами (через Secrets / env variables)
- ✅ Логирование и анализ вызовов модели в LangSmith

---

## 🧪 Пример результата

Модель возвращает объект вида:

```python
Person(
  name="Анна Смирнова",
  age=28,
  hobbies=["бег", "йога"]
)

🚀 Запуск

Ноутбук предназначен для запуска в Google Colab.

Перед запуском необходимо:

Добавить API-ключи в Colab Secrets

OPENAI_API_KEY

LANGCHAIN_API_KEY

Включить доступ секретов для блокнота

Запустить ноутбук сверху вниз

📊 Observability

Для анализа запросов используется LangSmith:

просмотр истории вызовов

latency

количество токенов

ошибки и успешные запуски

📝 Примечание

Учебные и устаревшие примеры (LLMChain, legacy agents, debug-код) были удалены, чтобы сохранить чистую и актуальную архитектуру LangChain.
👩‍💻 Автор

Ekaterina Luoto
Prompt Engineer / LLM & LangChain Enthusiast
