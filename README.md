# LangChain: Structured Output & Tracing (LangSmith)

## Описание
Ноутбук демонстрирует подключение LLM через **LangChain (LCEL)** и получение **структурированного ответа** с помощью `PydanticOutputParser`, а также трассировку запросов в **LangSmith**.

## Технологии
- Python
- LangChain (LCEL: `langchain_core`, `langchain_openai`)
- OpenAI API
- Pydantic
- LangSmith (Tracing / Observability)
- Google Colab (Secrets / env variables)

## Что реализовано
- Подключение модели `gpt-4o-mini` через `ChatOpenAI`
- Формирование запроса через `PromptTemplate`
- Структурированный вывод через `PydanticOutputParser`
- Результат в виде Python-объекта (Pydantic model)
- Безопасная работа с API-ключами (Colab Secrets / env vars)
- Логирование и анализ вызовов модели в LangSmith

## Пример результата
```python
Person(
    name="Анна Смирнова",
    age=28,
    hobbies=["бег", "йога"]
)
🚀 Запуск

Ноутбук предназначен для запуска в Google Colab.

Перед запуском необходимо:

Добавить API-ключи в Colab Secrets

OPENAI_API_KEY

LANGCHAIN_API_KEY

Включить доступ секретов для блокнота

Запустить ноутбук сверху вниз

📊 Observability

Для анализа запросов используется LangSmith:

просмотр истории вызовов

latency

количество токенов

ошибки и успешные запуски

📝 Примечание

Учебные и устаревшие примеры (LLMChain, legacy agents, debug-код) были удалены, чтобы сохранить чистую и актуальную архитектуру LangChain.

👩‍💻 Автор

Ekaterina Luoto
Prompt Engineer / LLM & LangChain Enthusiast

